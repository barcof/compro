"use client";
import { TypeAnimation } from "react-type-animation";
import type { Lang } from "./site-data";
import { copy } from "./site-data";
export default function Hero({lang="id"}:{lang?:Lang}) { const c=copy[lang]; return <section id="home" className="relative overflow-hidden bg-background min-h-screen"><div className="relative h-screen w-full overflow-hidden flex items-center justify-center"><video preload="auto" autoPlay muted playsInline loop className="absolute inset-0 w-full h-full object-cover z-0"><source src="/uploads/hero_video.mp4" type="video/mp4" /></video><div className="absolute inset-0 bg-black/50 z-10"/><div className="relative z-20 text-center text-white px-4 max-w-4xl mx-auto space-y-6"><h1 className="text-4xl sm:text-5xl md:text-6xl font-extrabold tracking-tight leading-tight">{c.hero.title}</h1><TypeAnimation sequence={[c.hero.text,10000,"",100]} speed={50} wrapper="p" cursor={false} repeat={Infinity} className="text-lg sm:text-xl md:text-2xl text-white/90 max-w-2xl mx-auto"/></div></div></section> }
